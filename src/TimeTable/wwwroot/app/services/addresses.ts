export namespace Addr {
    export class Auth {
        public static base = "api/auth/";
        public static Login = Auth.base + "login";
        public static Logout = Auth.base + "logout";
        public static Register = Auth.base + "register";
    }
}